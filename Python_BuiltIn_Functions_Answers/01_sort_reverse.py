# Sort ascending, descending, and print reversed order
numbers = [5, 2, 9, 1, 7]

print("Original:", numbers)
print("Ascending:", sorted(numbers))
print("Descending:", sorted(numbers, reverse=True))
print("Reversed order:", list(reversed(numbers)))
